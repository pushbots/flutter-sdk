name: pushbots_flutter_example
description: Demonstrates how to use the pushbots_flutter plugin.
publish_to: 'none'

environment:
  sdk: ">=2.1.0 <3.0.0"
  flutter: ">=0.1.4 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  pushbots_flutter:
    path: ../

flutter:
  uses-material-design: true